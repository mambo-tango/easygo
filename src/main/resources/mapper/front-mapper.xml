<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.front.dao.FrontDbDao">
	
	<select id="loadAllTrack" resultType="Track"> 
		select id, waybill_no, org_code, op_code, upload_time, create_time, error_message  from  t_exp_op_record_container_unsuccess ;
	</select>
	
</mapper>

